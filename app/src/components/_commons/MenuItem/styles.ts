import fontSizes from 'assets/tokens/styledComponent/_fontSize';
import styled from 'styled-components';

const MenuItemContainer = styled.div`
  .MuiMenuItem-root {
    font-size: ${fontSizes.bodySm} !important;
  }
`;

export default MenuItemContainer;
